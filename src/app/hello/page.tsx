import {Metadata} from "next";

export const metadata:Metadata = {
    title:'hello',
}

const Hello = () => {
    return (
        <>
            <div>
                hello
            </div>
        </>

    );
};

export default Hello;